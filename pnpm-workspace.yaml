packages:
  - "apps/*"
  - "packages/*"

# Only these packages may run install scripts. Lifted from the site's own
# package.json: contentlayer needs its postinstall to generate types.
onlyBuiltDependencies:
  - contentlayer
